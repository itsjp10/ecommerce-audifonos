import { Router } from "express";

const router = Router();

router.post("/login", (req, res) => {}); //acceder
router.post("/logout", (req, res) => {});
router.get("/protected", (req, res) => {});

export default router;
 